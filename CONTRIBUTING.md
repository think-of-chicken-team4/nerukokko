# チーム開発ルール

ねるコッコは6人（SW 3人・HW 3人）で開発しています。お互いの作業を壊さないために、次のルールを守ってください。

**開発を始める前に、このファイルと `CLAUDE.md` を必ず読んでください。**

| 文書 | 内容 |
|---|---|
| `CONTRIBUTING.md`（このファイル） | ブランチ・コミット・PR などのチームのルール |
| `CLAUDE.md` | 開発全体のルール（AI も人も同じルールで作業する） |
| `docs/dev-plan.md` | 要件・画面仕様・タスク一覧・決定事項 |
| `docs/api-spec.md` | 鶏・Webアプリとサーバーの通信の決まり |
| `docs/ble-protocol.md` | 鶏とたまごの BLE 通信の決まり |
| `docs/hw-verification.md` | 実機での検証・調整の一覧と手順 |

## 0. AI と人の役割分担

- **コードは AI（Claude Code など）に書かせてよい。** 実機を使った検証・調整は AI にはできないので、**ソフトウェア担当で分担して行う**（HW 担当は筐体の設計・3D プリントを担当）。
- 実機検証の項目と手順は `docs/hw-verification.md` にまとめている。測った値やうまくいかなかったことは、Issue か PR にコメントで残す（AI がコードを直すときの材料になる）。

### AI を使って開発するとき

- **Claude Code**：リポジトリのフォルダで起動すれば `CLAUDE.md` を自動で読み込む。念のため、最初に「CLAUDE.md と CONTRIBUTING.md を読んでから作業して」と伝える。
- **ほかの AI（ChatGPT・Copilot・Cursor など）**：`CLAUDE.md` を自動では読まないので、**最初に `CLAUDE.md` と `CONTRIBUTING.md` の内容を読ませてから**作業を頼む。
- AI が書いたコードも、自分で中身を確認してから PR を出す。実機で確認していないことは「未確認」と PR に書く。

## 1. ブランチ

| ブランチ | 用途 |
|---|---|
| `main` | 全員の成果をまとめる、常に動く状態のブランチ。**直接 push はできない**（保護設定済み） |
| 個人ブランチ（`aryu`、`ren` など） | 各自の作業用。自分のブランチ以外には push しない |
| `<名前>-<トピック>`（例：`aryu-login-page`） | 大きめの作業を分けたいときに個人ブランチから切る（任意）。`aryu` ブランチがあると Git の仕様で `aryu/〜` は作れないので、区切りは `-` にする |

## 2. 毎日の作業の流れ

**初回だけ**

```bash
git clone https://github.com/think-of-chicken-team4/nerukokko.git
cd nerukokko
git switch -c <自分の名前> origin/main
git push -u origin <自分の名前>
```

**作業を始めるとき**（レビューで追加された修正と main の最新を取り込む）

```bash
git switch <自分の名前>
git pull
git fetch origin
git merge origin/main
```

`git pull` は、レビューのときに自分のブランチへ修正コミットが追加されている場合に、それを取り込むためのものです。

**作業が一区切りしたとき**

```bash
git add <変更したファイル>
git commit -m "feat(app): 設定画面にアラーム編集フォームを追加"
git push origin <自分の名前>
```

GitHub で Pull Request（`<自分の名前>` → `main`）を作ります。

**PR がマージされたあと**：もう一度「作業を始めるとき」の手順で main を取り込みます。

## 3. コミットメッセージ

`<種類>(<範囲>): <日本語の要約>` の形で書きます。

| 種類 | 使うとき | 範囲 | 対象 |
|---|---|---|---|
| `feat` | 機能の追加 | `app` | Webアプリ |
| `fix` | バグ修正 | `server` | Supabase（DB・Edge Functions） |
| `docs` | ドキュメント | `egg` | たまごFW |
| `refactor` | 動作を変えない整理 | `chicken` | 鶏ユニット |
| `test` | テスト | `docs` | 仕様書など |
| `chore` | 設定・雑務 | | |

例：`fix(egg): 充電検知のチャタリング対策を修正`

## 4. Pull Request

- **修正や機能追加は、必ず PR にして main に入れる**（個人ブランチに push しただけでは main に入らない）。
- 1つの PR には1つの目的（機能・修正）だけを入れる。大きくなりすぎたら分ける。
- PR テンプレートに沿って「何を・なぜ・どう確認したか」を書く。**FW は実機で何を確認したかを必ず書く**（レビューする側は実機を触れないため）。
- 担当外のディレクトリを変えた場合は、その担当者にも知らせる。
- 通信の契約（`docs/api-spec.md`・`docs/ble-protocol.md`・DBマイグレーション）を変える PR は、影響を受ける担当者全員に知らせる。

### レビューとマージの流れ

1. PR を出したら、田村（PM）に連絡する。
2. 田村が Claude Code でレビューし、結果が PR にコメントされる。指摘は3段階に分けて書く。
   - **M**：マージ前に必須
   - **S**：あとで直すもの。Issue にして作者に割り当てる
   - **提案**：任意
3. 結果に応じて次のどれかになる。
   - **M なし** → そのままマージ
   - **軽微な M**（誤字・ドキュメントの不整合など） → レビュー側があなたのブランチに修正コミットを追加してからマージ。作業を再開する前に `git pull` すること
   - **実機確認や設計判断が必要な M** → あなたに修正を依頼する。直したら同じブランチに push すれば、同じ PR に反映される
4. マージ方法は **Create a merge commit**（個人ブランチを使い続けるため、Squash は使わない）。

## 5. 担当ディレクトリ

| ディレクトリ | 内容 | 担当 |
|---|---|---|
| `app/` | Webアプリ（Next.js） | 田村 |
| `server/` | Supabase（DB・Edge Functions） | 田村 |
| `firmware/egg/` | たまごFW（ESP32） | 田村（実機検証は岡田） |
| `firmware/chicken/` | 鶏ユニット（Raspberry Pi） | 田村（実機検証は浅井・岡田） |
| `docs/` | 仕様書・設計資料 | 全員 |
| 実機検証（`docs/hw-verification.md`） | たまご・鶏・結合テスト | 岡田（たまご中心）・浅井（鶏中心） |

Webアプリ・バックエンド・鶏／たまごのソフトは、まず Claude Code で全体の叩き台を作り、そのあと直す箇所を担当者で分けて修正します。

## 6. 秘密情報（このリポジトリは公開されています）

- APIキー、パスワード、トークン、Wi-Fi の SSID・パスワード、個人のメールアドレスは**絶対にコミットしない**。
- 秘密情報は `.env` や `.env.local` に書く（`.gitignore` 済み）。必要な変数名は `.env.example` にキー名だけ書いて共有する。
- 間違ってコミットしたら、すぐにチームに連絡し、そのキーを無効化して作り直す（履歴から消すだけでは不十分）。

## 7. タスク管理

- タスクは `docs/dev-plan.md` のタスク一覧（`T-xxx`）と GitHub Issue で管理する。
- Issue にはラベル `frontend` / `backend` / `firmware` を付ける。
- 仕様について決めたことは `docs/dev-plan.md` の「決定ログ」に日付付きで残す。
